> # ℹ️ 용어 업데이트 안내 (현재 구현 기준)
> 이 문서는 초기 기획안입니다. 현재 구현 용어/이모지로 읽어 주세요:
> **인싸 → 베프(💖)**, **일반 → 부끄럼(🙈)**, **불편/얼음 → 잠수(🥷)**.
> 친구별 개별 오버라이드는 🎯 항상 정확 / 🥷 항상 잠수 / 🔗 그룹 따름.
> 최신 정책은 `CLAUDE.md` 와 `docs/01,05` 참고.

\---

# \[기획서] PinFlick 친구추가 및 관계 관리 시스템 (3단계 본심 필터)

## 1\. 개요 (Overview)

* **목적:** 위치 기반 서비스의 사생활 노출 우려를 해소하고, 유저 간의 친밀도에 기반한 차별화된 위치 공유 경험을 제공함.
* **핵심 컨셉:** 복잡한 사회적 관계를 유저의 본심에 따른 3단계 시스템(**베프 / 일반 / 불편**)으로 단순화하여 직관적인 UI/UX와 명확한 위치 권한 제어를 구현함.
* **통신 방식:** 상시 백그라운드 추적을 지양하고, 앱 실행 시점 및 상호작용 기반의 양방향 수락제(Two-way Opt-in) 시스템 채택.

\---

## 2\. 관계 정의 및 위치 노출 정책 (Relation \& Privacy Policy)

|관계 등급 (UI 명칭)|관계 성립 조건|지도 위 내 핀(Pin) 노출 정책|비고|
|-|-|-|-|
|**베프** (`내 심장 🫀`)|양방향 친구 수락 완료|**정확한 실시간 위치 (cm/m 단위)**<br>||

<br>· 기기 배터리 상태, 이동 속도 노출<br>

<br>· 실시간 인터랙션(이모지 폭탄) 허용 | 가족, 연인, 절친 대상 |
| **일반** (`사회적 체면 🕶️`) | 양방향 친구 수락 완료 | **흐릿한 위치 (안개 모드 ☁️)**<br>

<br>· 상세 주소 비공개, 대략적인 동네 반경만 표시<br>

<br>· (옵션) 특정 시간대(예: 퇴근 후) 자동 안개 전환 | 학원, 직장, 동호회 대상 |
| **불편** (`CCTV 조심 🧊`) | **단방향 지정** (나만 설정)<br>

<br>※ 상대방은 알 수 없음 | **과거 위치 박제 (얼음 모드 ⏸️)**<br>

<br>· 내가 지정한 시점의 마지막 위치로 핀이 고정됨<br>

<br>· 상대가 내 근처 접근 시 내게만 '레이더 알림' 발송 | 차단/회피 대상 |

\---

## 3\. 핵심 기능 요구사항 (Functional Requirements)

### 3.1 친구 신청 및 수락 (양방향)

* **F-01-01 (신청):** 유저는 고유 ID, 연락처 동기화, 또는 QR 코드를 통해 다른 유저에게 친구 요청을 보낼 수 있다.
* **F-01-02 (수락):** 상대방이 요청을 수락하기 전까지는 서로의 위치나 피드가 공유되지 않는다 (Pending 상태).
* **F-01-03 (초기 설정):** 친구 수락 완료 즉시, 유저에게 해당 친구의 등급(`\\\[베프] / \\\[일반]`)을 선택하는 팝업 UI를 노출한다. (기본값은 `\\\[일반]`)

### 3.2 관계 등급 관리 (그루핑)

* **F-02-01 (등급 변경):** 유저는 친구 목록 및 지도 UI에서 언제든지 특정 친구의 등급을 `\\\[베프 / 일반 / 불편]`으로 실시간 변경할 수 있어야 한다.
* **F-02-02 (불편 지정):** 기존 친구를 `\\\[불편]` 등급으로 변경할 경우, 상대방에게는 어떠한 알림도 가지 않아야 하며 상대방 화면에는 내 위치가 즉시 얼음(Pause) 상태로 고정된다.

### 3.3 그룹별 지도 필터링 (Map Filtering)

* **F-03-01 (상단 탭 필터):** 지도(Map) 화면 상단에 `\\\[전체] \\\[베프] \\\[일반]` 필터 탭을 제공한다.
* **F-03-02 (렌더링 제어):** 필터 선택 시 지도 위에 해당 등급에 속한 친구들의 핀(Pin)만 필터링하여 렌더링한다. (`\\\[불편]` 등급은 내 지도에서 원천 배제)

\---

## 4\. 데이터베이스(DB) 구조 설계안 (Data Structure Example)

Firebase Firestore 또는 NoSQL 환경을 고려한 스키마 구조안입니다.

### 4.1 `users` 컬렉션 문서 (User Document)

```json
{
  "uid": "user\\\_id\\\_01",
  "email": "jaduck9@gmail.com",
  "nickname": "방랑자",
  "current\\\_location": {
    "latitude": 37.3456,
    "longitude": 126.9876,
    "updated\\\_at": "2026-05-28T15:56:00Z"
  },
  "battery\\\_status": 85
}

```

### 4.2 `friendships` 컬렉션 문서 (Relationship Document)

두 유저 간의 상태와 각자가 상대를 생각하는 등급(Rel\_Type)을 독립적으로 저장하여 단방향 '불편' 모드를 구현합니다.

```json
{
  "friendship\\\_id": "ship\\\_abc123",
  "user\\\_ids": \\\["user\\\_id\\\_01", "user\\\_id\\\_02"],
  "status": "ACCEPTED", // PENDING, ACCEPTED, BLOCKED
  "created\\\_at": "2026-05-20T10:00:00Z",
  
  "detail\\\_settings": {
    "user\\\_id\\\_01": { 
      "nickname\\\_by\\\_me": "김철수",
      "rel\\\_type": "BEST" // user\\\_01이 보기에 user\\\_02는 '베프'
    },
    "user\\\_id\\\_02": {
      "nickname\\\_by\\\_me": "구대표님",
      "rel\\\_type": "BAD"  // user\\\_02가 보기에 user\\\_01은 '불편(얼음)'
    }
  }
}

```

\---

## 5\. 프론트엔드 위치 렌더링 로직 (Pseudocode)

개발자가 API 호출 후 지도에 위치를 뿌릴 때 써야 하는 핵심 로직 예시입니다.

```javascript
// 상대방(Friend)의 위치를 내 지도에 그릴 때의 로직
function renderFriendPinOnMap(myId, friendId, friendshipDoc, friendLocation) {
  const mySetting = friendshipDoc.detail\\\_settings\\\[friendId];    // 상대가 나를 지정한 설정
  const friendSetting = friendshipDoc.detail\\\_settings\\\[myId];  // 내가 상대를 지정한 설정

  // 1. 상대방이 나를 '불편(BAD)'으로 지정했거나 내가 상대를 '불편(BAD)'으로 지정한 경우
  if (mySetting.rel\\\_type === 'BAD') {
    // 상대방 화면에 나는 '얼음 모드'로 고정되어 보여야 함 (마지막 저장된 위치 렌더링)
    return renderFrozenPin(friendLocation.frozen\\\_lat, friendLocation.frozen\\\_lng);
  }
  
  // 2. 상대방이 나를 '일반(NORMAL)'으로 지정한 경우 (안개 모드)
  if (mySetting.rel\\\_type === 'NORMAL') {
    // 좌표값에 의도적인 오차(Noise)를 주거나, 행정동 중심 좌표로 흐릿하게 렌더링
    const blurredLocation = applyBlurEffect(friendLocation.latitude, friendLocation.longitude);
    return renderBlurredPin(blurredLocation);
  }

  // 3. 상대방이 나를 '베프(BEST)'로 지정한 경우
  if (mySetting.rel\\\_type === 'BEST') {
    // 정확한 실시간 위치 및 배터리 정보 정밀 렌더링
    return renderExactPin(friendLocation.latitude, friendLocation.longitude, friendLocation.battery);
  }
}

```

\---


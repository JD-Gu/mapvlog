[개발 지시서] 실시간 위치 갱신 주기 최적화 및 UI 시간 표시 구현
1. 목적
배터리 최적화: 유저의 상태(앱 활성화 여부 및 이동 상태)에 따라 위치 수집 주기를 동적으로 조절하여 불필요한 배터리 소모를 방지함.

Live 감성 극대화: 친구의 위치가 언제 업데이트되었는지 지도 UI에 명시하여 데이터의 신뢰성을 높이고 서비스의 실시간성을 시각화함.

2. 백엔드 데이터 구조 가이드 (DB Schema)
각 유저의 최신 위치를 저장하는 locations 데이터 모델에 위치 수집 당시의 타임스탬프(updated_at) 필드를 반드시 포함해야 합니다.

Firestore / NoSQL 예시:

JSON
{
  "uid": "user_id_01",
  "latitude": 37.3456,
  "longitude": 126.9876,
  "updated_at": "2026-05-29T08:45:00Z", // ISO 8601 UTC 타임스탬프 필수
  "is_moving": true,                    // 클라이언트 센서 기반 이동 여부 플래그
  "battery_level": 88                   // 배터리 잔량 잔여 플래그 (선택)
}
3. 클라이언트 위치 갱신 주기 로직 (Dynamic Interval)
OS(iOS/Android)의 위치 서비스 및 타이머를 활용하여 아래 조건에 따라 백엔드로의 POST/UPDATE 주기를 제어합니다.

3.1 앱이 포그라운드(Foreground) 상태일 때 (지도 화면 활성화)
기본 정책: 유저가 앱을 켜고 지도를 보고 있을 때는 최상의 실시간성을 유지함.

갱신 주기: 30초 지정 주행 스트리밍

로직: setInterval 또는 LocationListener를 통해 30초마다 현재 좌표를 DB에 업데이트함.

3.2 앱이 백그라운드(Background) 상태일 때 (주머니 속 상태)
기본 정책: 배터리 보존을 위해 센서 정보(가속도/Geofence)와 연동하여 주기를 늘림.

가속도 기반 가변 주기 규칙:

이동 중 (is_moving == true): 대중교통, 자동차 이동 및 도보 이동 감지 시 3분 ~ 5분 주기 또는 반경 150m 이동 시 갱신.

정지 상태 (is_moving == false): 특정 장소(예: 사무실, 집)에 10분 이상 체류 시 15분 ~ 20분 주기로 대폭 전환.

예외 처리: 기기 배터리가 15% 이하일 경우, 백그라운드 갱신 주기를 30분으로 강제 고정하여 배터리 드레인을 방지함.

4. 프론트엔드 UI/UX: 최근 갱신 시간 표시 명세
현재 PinFlick의 메인 지도 화면에 렌더링되는 친구 핀(Pin)의 말풍선 UI에 아래 갱신 시간 변환 로직(Time-ago)을 적용합니다.

4.1 갱신 시간 계산 및 포맷팅 룰 (Client-side)
서버에서 받아온 updated_at 시간과 현재 디바이스 시간(Current_Time)의 차이를 계산하여 아래 규칙으로 문자열을 파싱합니다.

Diff < 1분: 방금 전 또는 ● 라이브 (초록색 도트 표시)

1분 ≤ Diff < 60분: N분 전 (예: 3분 전, 45분 전)

1시간 ≤ Diff < 24시간: N시간 전 (예: 2시간 전)

24시간 ≤ Diff: 어제 또는 오래전

4.2 UI 배치 가이드
현재 지도 위에 표시되는 유저 아바타(예: '구자덕', '방랑자') 프로필 서클 하단 영역에 텍스트 레이블을 추가합니다.

시각화 예시:

[방랑자 프로필 사진] ➡️ 하단 텍스트: 방랑자 · 방금 전

[구자덕 프로필 사진] ➡️ 하단 텍스트: 구자덕 · 12분 전

디자인 가이드: 지도 레이어 위에서 시인성을 확보할 수 있도록 글자 크기는 10~11pt 내외로 작게 조정하고, 가벼운 드롭 섀도(Drop Shadow)나 반투명한 흰색 둥근 배경 칩(Badge) 형태로 처리해 주세요.

5. 구현 우선순위 (Roadmap)
Sprint 1: updated_at 타임스탬프 DB 연동 및 포그라운드(30초) 위치 전송 로직 구현.

Sprint 2: 지도 위 친구 아바타 하단에 방금 전 / N분 전 실시간 텍스트 파싱 렌더링.

Sprint 3: 백그라운드 가속도 기반 가변 주기(5분/20분) 및 배터리 절전 모드 고도화.